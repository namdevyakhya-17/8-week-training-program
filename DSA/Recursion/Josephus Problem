class Solution {
    // Josephus Problem
    public int findWinner(int n, int k){
        if(n == 1) return 0;
        int ans = (findWinner(n-1, k) + k ) % n;

        return ans;
    }
    public int findTheWinner(int n, int k) {
        return findWinner(n,k) + 1;
    }
}
